{
  "permissions": {
    "defaultMode": "auto"
  },
  "model": "opus[1m]",
  "hooks": {
    "WorktreeCreate": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "~/.claude/hooks/worktree-create.sh",
            "statusMessage": "Creating worktree in .worktrees/"
          }
        ]
      }
    ],
    "WorktreeRemove": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "~/.claude/hooks/worktree-remove.sh"
          }
        ]
      }
    ]
  },
  "statusLine": {
    "type": "command",
    "command": "sh ~/.claude/statusline-command.sh"
  },
  "enabledPlugins": {
    "swift-lsp@claude-plugins-official": true,
    "superpowers@claude-plugins-official": true,
    "code-simplifier@claude-plugins-official": false,
    "feature-dev@claude-plugins-official": false,
    "commit-commands@claude-plugins-official": false,
    "frontend-design@claude-plugins-official": false,
    "playwright@claude-plugins-official": false,
    "github@claude-plugins-official": true
  },
  "alwaysThinkingEnabled": true,
  "effortLevel": "xhigh",
  "modelSettings": {
    "claude-fable-5-1": {
      "effortLevel": "high"
    }
  },
  "tui": "fullscreen",
  "theme": "auto",
  "preferredNotifChannel": "terminal_bell",
  "inputNeededNotifEnabled": true,
  "agentPushNotifEnabled": true,
  "autoMode": {
    "environment": [
      "### Org-wide",
      "**Organization**: None configured",
      "**Cloud provider(s)**: None configured",
      "**Repository visibility**: private — jaylaney/catalano-notes (confirmed via gh)",
      "**Internal sharing / snippet hosting**: None configured — treat public paste/gist services as outside the trust boundary",
      "**Secrets management**: None configured — repo uses ignored local secrets file (Shared/AI/Secrets.swift) and CI secret CLAUDE_CODE_OAUTH_TOKEN (name only, not its value)",
      "**Default / protected branches**: main (default); no branch protection rules or rulesets listed via gh — treat main as unprotected at the platform level, but per CLAUDE.md untested work must not land on main directly",
      "**CI/CD deploy targets**: None configured",
      "**Network posture**: None configured",
      "**Source control**: The trusted repo (jaylaney/catalano-notes) and its origin remote (github.com:jaylaney/catalano-notes.git) only — no additional orgs configured",
      "**Trusted internal domains**: None configured",
      "**Trusted cloud buckets**: None configured",
      "**Key internal services**: None configured",
      "**Internal package registry**: None configured",
      "**Sensitive data locations & audiences**: Shared/AI/Secrets.swift (ignored local AI API key file); CloudKit master encryption key in Keychain (iCloud-synced); E2EE'd Block.content and Page.title — share only with audiences cleared at the [named+specifics] bar",
      "**Data retention / declassification**: None configured",
      "**Sensitive remote targets**: any namespace, host, or container whose name carries `prod` or `production` as a whole word or name segment; note the CloudKit container `iCloud.com.jaylaney.Catalano` has separate Development/Production environments per build config — treat the Production CloudKit environment as a sensitive remote target",
      "**Protected deployment namespaces / environments**: None configured — fall back to the Sensitive remote targets heuristic",
      "**Protected IaC scopes**: IAM, RBAC, networking, quota, and node-pool resources; anything whose name or tag carries `prod` or `production` as a whole word or name segment",
      "### User-specific",
      "**Primary use of Claude Code**: software development (Swift/SwiftUI iOS+macOS notes app)",
      "**Trusted repo**: /Users/jay/Development/catalano-notes (jaylaney/catalano-notes, private) and its origin remote — confidential material (e.g. build/config details) is fine to commit here; secrets and credentials (Secrets.swift, API keys, CLAUDE_CODE_OAUTH_TOKEN) are never cleared into the repo by its private visibility",
      "**Org-specific CLIs**: None configured — xcodebuild, xcrun, perl, swiftc, tmutil, xcodebuildmcp are standard iOS/macOS toolchain usage, not org-specific",
      "routine under /Users/jay/Development/catalano-notes/ prefix: xcodebuild build/test invocations with separate -derivedDataPath per CLAUDE.md guidance"
    ]
  }
}
